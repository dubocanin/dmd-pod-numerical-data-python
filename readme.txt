1. In the folder data, extract the .npy from the rar archives prior to running the code.
2. It is necessary to install the modred library before running the scripts (command below):
	pip install modred
